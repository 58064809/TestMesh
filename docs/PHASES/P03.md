# P03：OpenHands 与工程上下文

- **状态**：已规划，禁止提前实现
- **目标**：接入 OpenHands，并增加 Repo/Code/Log/Terminal/Docker 工程上下文。

## 范围

- OpenHands 单一路径集成。
- 工程上下文选择、查看和任务关联。
- 结果与证据回写 TestMesh。

## 验收标准

用户可从 TestMesh 发起基于仓库、代码、日志、终端和 Docker 上下文的工程任务，并查看可追溯结果。

## 明确不做

自研 Agent Loop、Harness Runtime、Checkpoint/Recovery、Tool Executor；P04 引擎；P05 质量治理。

## 依赖

P02 已完成；OpenHands 部署、权限与数据边界获批准。

## 下一阶段入口

P03 全部验收通过并获用户批准后进入 P04。

