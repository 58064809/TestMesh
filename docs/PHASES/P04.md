# P04：UI、APP、性能与安全测试

- **状态**：阻塞；P04-A 已完成（2026-09-02），P04-B、P04-C 已完成（2026-09-03），P04-D 于 2026-09-03 阻塞在 Windows Defender 安全拦截
- **目标**：复用成熟引擎扩展四类测试能力。

## 范围

- UI：Playwright。
- APP：Appium。
- 性能：k6。
- 安全：ZAP。
- 统一 TestRun 与 Evidence 展示。

## 验收标准

每类测试均有且只有一条可运行路径；结果和失败证据可在 TestMesh 中查看并追溯。

## 明确不做

自研 Runner、同类备用引擎、静默降级、P05 测试设计与用例生成、P06 知识检索与 Quality Gate。

## 依赖

P03 已完成；四个引擎的版本、运行环境和授权方案逐项获批准。

## 下一阶段入口

P04 全部验收通过并获用户批准后进入 P05。

## P04-A：Playwright UI 测试固定路径

- 版本：`@playwright/test@1.62.1`，Chromium only。
- 镜像：`mcr.microsoft.com/playwright:v1.62.1-noble`；一任务一临时容器。
- 目标项目必须在 `package.json` 和已安装依赖中同时明确使用 `1.62.1`；不匹配立即停止。
- 仅挂载用户选择的仓库到 `/workspace:ro` 和本次 artifact 目录到 `/artifacts:rw`；不挂载 Docker Socket 或其他宿主目录。
- 每次运行必须获得用户明确授权。
- 固定 single worker、retries=0、`chromium` project；不使用其他浏览器或备用 Runner。
- JSON Reporter 是唯一 TestRun 结果来源；Trace 使用 `retain-on-failure`，仅失败时作为 Evidence 保存。
- `baseURL`、认证、Header 和 fixture 由目标项目的 Playwright 配置负责。
- TestMesh 不修改目标项目的测试代码、依赖或配置。

### P04-A 验收标准

用户选择仓库和一个 Playwright 测试文件并明确授权，TestMesh 在一次性容器中执行该测试，从 JSON Reporter 保存状态、用例、耗时和错误；失败时将 Trace 关联为 Evidence；结果可在工作台查看，容器结束后已删除，目标仓库无改动。

### P04-A 验收记录

2026-09-02 使用临时 Git 仓库 `D:\TestHome\TestMeshPlaywrightDemo`，通过同一条固定 Docker 路径分别执行一条通过用例和一条故意失败用例：

- 官方镜像：`mcr.microsoft.com/playwright:v1.62.1-noble`；digest `sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e`。
- 通过任务 ID：`abcea8a6-7249-44af-9a40-c9e38e15b1fa`；JSON Reporter 保存 1 通过、0 失败、0 跳过；退出码 0；成功任务无 Trace。
- 失败任务 ID：`8996fe43-e6d0-4844-b6c0-ff960e8488ea`；JSON Reporter 保存 0 通过、1 失败、0 跳过；退出码 1。
- 失败 Trace Evidence：artifact ID `c16960ac-1604-4d4e-9435-92531a2b2aba`；下载接口返回 `application/zip`，大小 158,114 bytes。
- 两次任务均为 Chromium、single worker、retries=0；容器结束后已删除。
- 验收仓库 `git status --short` 为空，TestMesh 未修改其代码、依赖或配置。
- 工作台实测可读取仓库和 2 个测试文件；只有选择文件并勾选明确授权后，运行按钮才启用；结果和 Trace 数量可见。
- SQLite 原生模块修复后，`npm test` 通过 3 个测试文件、11 项测试；`npm run lint` 与 `npm run build` 通过。

## P04-B：Android Emulator + Appium 固定路径

- 仅支持本机已启动的 Android Emulator；不支持真机、iOS、设备农场、云设备或设备 fallback。
- 固定 Appium Server `3.7.0` 与 UiAutomator2 Driver `8.5.2`，服务仅监听 `127.0.0.1:4723`。
- 目标测试项目固定使用 WebdriverIO `9.31.5`、`@wdio/cli` `9.31.5`、`@wdio/local-runner` `9.31.5`、`@wdio/mocha-framework` `9.31.5` 与 `@wdio/junit-reporter` `9.31.2`；声明或安装版本不匹配立即停止。
- TestMesh 在运行前要求且只允许一个状态为 `device`、序列号为 `emulator-*`、`sys.boot_completed=1` 的设备；物理设备或多设备存在时停止。
- 每次运行必须获得用户明确授权；固定 single device、single session、`maxInstances=1`、`specFileRetries=0`、`connectionRetryCount=0`、Mocha retries=0。
- JUnit Reporter 是唯一结构化 TestRun 结果来源；终端输出与 Appium 日志保存，失败时必须同时保存截图和 Page Source，否则任务标记为运行错误。
- 测试项目自己的 WebdriverIO 配置负责 app capability 与测试逻辑；TestMesh 不自动安装、升级或修改目标项目。

### P04-B 验收环境

- 安装根目录：`D:\TestHome\Android`；Android SDK、AVD、JDK、Appium Server、UiAutomator2 Driver、npm cache 和临时目录均在 D 盘。
- Android SDK Platform 35、Android Emulator `37.1.11`、platform-tools `37.0.1`、Temurin JDK `17.0.20.1`。
- AVD：`TestMesh_API_35`，Android 15 / API 35 / x86_64；验收设备序列号 `emulator-5554`。
- 验收仓库：`D:\TestHome\TestMeshAppiumDemo`；包含一条通过用例和一条用于验证失败证据的故意失败用例。

### P04-B 验收记录

2026-09-03 通过同一条固定 Appium + UiAutomator2 + WebdriverIO 路径完成真实验收：

- 通过任务 ID：`62138760-e423-4e6f-93cf-30289c83aa8e`；JUnit Reporter 保存 1 通过、0 失败、0 跳过；退出码 0；保存 Appium 日志，并确认 CLI 生效 `--mochaOpts.retries 0`。
- 失败任务 ID：`91219acb-ad66-4cc2-87a4-57ed2958da59`；JUnit Reporter 保存 0 通过、1 失败、0 跳过；退出码 1。
- 失败截图：artifact ID `87314cee-0b90-4b3c-b521-68fb3848cc6f`，`image/png`，149,912 bytes。
- Page Source：artifact ID `f4adfd6f-b19f-4508-9127-7625d7b04347`，`application/xml`，41,393 bytes。
- Appium 日志：artifact ID `94e1834d-b816-4533-9e1d-476fa7dacab9`，`text/plain`，7,725 bytes。
- 工作台实测可见 APP 测试入口、Android/Appium/UiAutomator2 版本、TestRun 计数和中文“失败截图 / 页面源 / Appium 日志”下载入口。
- 每次任务结束后 `127.0.0.1:4723` 无监听；Emulator 保持在线；验收仓库 `git status --short` 为空。
- `npm test` 通过 4 个测试文件、13 项测试；`npm run lint` 与 `npm run build` 通过。

## P04-C：k6 性能测试固定路径

- 固定 k6 `2.2.0` Windows x64 官方独立二进制，安装在 `D:\TestHome\k6\2.2.0`；这是阶段 pin，升级必须单独获得批准。
- 下载缓存、临时目录、固定配置、summary 与 TestMesh artifact 均位于 D 盘。
- 用户选择一个 Git 仓库和仓库内单个 `.js` k6 脚本；TestMesh 不生成、修改或下载脚本。
- 脚本自行定义 VU、duration/scenario 与 threshold；TestMesh 不覆盖这些选项。
- 每次执行必须明确授权；脚本以当前 Windows 用户权限在宿主机运行，固定单进程、zero retry。
- `--summary-export` JSON 是唯一 TestRun 结构化来源；无 summary 时标记运行错误，禁止解析终端文本补数据。
- 保存终端输出与原始 summary JSON 两份 Evidence。
- 不接 Grafana Cloud、不做分布式、不安装扩展、不做 browser performance，不增加 Docker 或备用 Runner。

### P04-C 验收记录

2026-09-03 使用临时 Git 仓库 `D:\TestHome\TestMeshK6Demo`，通过同一条固定宿主机路径完成真实通过与阈值失败验收：

- 官方 k6 `2.2.0 windows/amd64` 已安装并通过官方 SHA256 校验；安装、下载和临时目录均在 `D:\TestHome\k6`。
- 通过任务 ID：`b4f6e4b3-8edf-4da0-86af-12e86019c24b`；3 个 HTTP 请求、3 次迭代、3 个检查通过、退出码 0，两个 threshold 均达标。
- 阈值失败任务 ID：`9c4e6477-899e-410e-9959-eee64a9196dc`；3 个 HTTP 请求、3 次迭代，`p(95)<0.001` 未达标，退出码 99，正确保存为“阈值失败”而非运行错误。
- 两次任务均保存 `terminal-output.txt` 与 `summary.json` Evidence；指标和 threshold 状态只取自 summary JSON。
- 工作台实测可见“性能测试”入口、宿主权限说明、k6 2.2.0、TestRun 指标、阈值结果、终端输出及 Evidence 下载入口。
- `npm test` 通过 5 个测试文件、16 项测试；`npm run lint` 与前后端 build 通过。

P04-C 到此停止。P04-D 已获得用户明确批准。

## P04-D：ZAP 无认证被动基线固定路径

- 固定 OWASP ZAP `2.17.0`，使用官方 `ZAP_2.17.0_Crossplatform.zip`，阶段批准 SHA-256 为 `94c8f767b1c2e94f0db66b3ae56514d5e3f5a728ee1b6c798e0c8fe2d61fbff0`。
- 固定安装目录 `D:\TestHome\ZAP\2.17.0`，复用 D 盘 JDK 17；下载、临时文件、ZAP home、报告和 Evidence 均在 D 盘。
- 一次运行固定为 Target URL → Traditional Spider（最多 1 分钟）→ Passive Scan Wait（最多 2 分钟）→ Traditional JSON Report。
- 每次运行必须确认用户有权扫描目标并明确授权；单 ZAP 进程、zero retry，结束后关闭。
- Traditional JSON 是唯一结构化 TestRun 来源；无报告或报告不可解析时标记运行错误，不解析终端文本补数据。
- 保存风险等级、告警名称、URL、参数、证据和修复建议；报告 JSON 与终端输出保存为 Evidence。
- 成功运行状态为“完成”并展示告警计数；P04-D 不定义阻断阈值，Quality Gate 属于 P06。
- Traditional Spider 对现代 JavaScript 应用覆盖有限，界面必须明确提示且不得自动切换 Client/AJAX Spider。
- 不做主动扫描、认证、API 导入、Client/AJAX Spider、云扫描、插件更新、Docker、Windows Installer 或备用运行路径。

### P04-D 阻塞记录

2026-09-03 已完成最小数据库、后端单一路径、中文工作台页面和报告解析测试；页面可见授权边界与 Traditional Spider 覆盖限制，未授权时运行按钮禁用。`npm test` 通过 6 个测试文件、20 项测试，lint 与前后端 build 通过。

官方 ZIP 通过同一 GitHub Release URL 下载至 D 盘并合并为批准大小 `286,652,857` bytes；在执行 SHA-256 校验时，Windows Defender 实时保护以 `Program:Java/Multiverze!rfn`（Threat ID `453479`，严重性“高”，类别“设置修改程序”）拦截文件，导致校验无法完成。TestMesh 已停止在该失败点：文件未解压、ZAP 未启动、真实扫描未执行、P04-D 未验收。未经用户明确批准不得添加 Defender 排除项、允许威胁、替换安装包、改用 Docker/Installer 或进入 P05。

误报复核证据、Microsoft URL 提交稿、ZAP 官方讨论组发帖稿以及恢复开发条件见 `docs/PHASES/P04-D-DEFENDER-REVIEW.md`。截至 2026-09-03，两份外部材料均未最终提交；P04-D 仍为 `BLOCKED`。
